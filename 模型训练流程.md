## 传输模型
从我们组服务器上传模型到矩池云服务器
scp -P 38959 /home/zsy/model/Llama-3-8B-Instruct_1.zip root@connect.nma1.seetacloud.com:/root/autodl-fs
scp -P 38959 /home/zsy/model/ds_with_metrics.zip root@connect.nma1.seetacloud.com:/root/autodl-fs

ssh -p 20302 root@connect.nma1.seetacloud.com

Llama-3-8B-Instruct



ssh -p 38959 root@connect.nma1.seetacloud.com
ssh -p 38959 root@connect.nma1.seetacloud.com
E7NeT760Rx4y
E7NeT760Rx4y
## 配置环境
conda create --name advan python=3.12.11
source ~/.bashrc
conda activate advan
pip install -r /requirement.txt

## 多卡训练设置

accelerate config



compute_environment: LOCAL_MACHINE

debug: false

distributed_type: FSDP

downcast_bf16: 'no'

fsdp_config:

  fsdp_activation_checkpointing: true  *# 显存足够时，如果想追求极致速度，甚至可以尝试设为 false (用显存换计算时间)，但在 4096 长度下建议保持 true*

  fsdp_auto_wrap_policy: TRANSFORMER_BASED_WRAP

  fsdp_transformer_layer_cls_to_wrap: LlamaDecoderLayer *# 必须加！*

  fsdp_backward_prefetch: BACKWARD_PRE

  fsdp_cpu_ram_efficient_loading: true

  fsdp_forward_prefetch: false

  fsdp_offload_params: false  *# 坚决选 false，A100 没必要用 CPU offload，会拖慢速度*

  fsdp_sharding_strategy: FULL_SHARD *# 保持 FULL_SHARD，最大化 Batch Size 空间*

  fsdp_state_dict_type: FULL_STATE_DICT *# 方便保存*

  fsdp_sync_module_states: true

  fsdp_use_orig_params: true

machine_rank: 0

main_training_function: main

mixed_precision: bf16

num_machines: 1

num_processes: 4

rdzv_backend: static

same_network: true

use_cpu: false



## 训练模型
export WANDB_MODE=offline
export NCCL_P2P_DISABLE=1
export NCCL_IB_DISABLE=1

accelerate launch /rlREMOVEDcode/code/implement_final_ppo_v3.py

nohup accelerate launch /rlREMOVEDcode/code/implement_final_ppo_v3.py > /root/autodl-fs/llama3-8b_ppo.log 2>&1 &

tail -f /train/logs/llama3-8b_advan_1e-5_no_kl_2_4096.log
pkill -f implement_final_ppo_v3.py

## 评估模型
安装Alpaca Eval环境

conda create -n ae python=3.10 -y
conda activate ae
pip install alpaca_eval==0.6.2 vllm==0.5.3 datasets==2.21.0

git clone https://github.com/princeton-nlp/SimPO.git

export HF_ENDPOINT=https://hf-mirror.com
alpaca_eval evaluate_from_model --model_configs /SimPO/eval/alpacaeval2/configs/Llama-3-Instruct-8B-SimPO.yaml --annotators_config weighted_alpaca_eval_gpt4_turbo

下载
log
results里的json文件

本地电脑上配置同样的环境
设置openai apikey

export OPENAI_API_KEY="REMOVEDproj-7KT6m1xepZjddLjOj2SCtRvM-u5TjDpNcO5PBtSUnbYlTg6itIZbIvnMqIk-P9B_AmbWrj4hznT3BlbkFJhSeP3_fGjnTZCxOouiCQsNKZCHvc3lsGr-Ldvq-wRbPudxeMFFL6Ts7XI-A5s_A5_1K_Tp5mQA"

alpaca_eval evaluate --model_outputs /Users/zzzsy/project/RLHF/code/rlREMOVEDcode/alpaca_outputs/f/ppo_kl/Llama-3-Instruct-8B_ppo_clip_0.1_1.0/model_outputs.json --annotators_config weighted_alpaca_eval_gpt4_turbo

WIN RATE详见weighted_alpaca_eval_gpt4_turbo/leaderboard.csv